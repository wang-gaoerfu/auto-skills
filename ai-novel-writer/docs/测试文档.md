# 墨飞小说创造 - 测试文档

## 测试环境准备

### 1. 安装测试依赖

```bash
npm install -D vitest @vitest/ui
```

### 2. 添加测试脚本

在 `package.json` 中添加：

```json
{
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage"
  }
}
```

---

## 测试流程

### 1. 启动项目

```bash
npm run dev
```

### 2. 手动测试清单

#### 用户注册流程
- [ ] 访问 http://localhost:3000/register
- [ ] 输入邮箱，点击发送验证码
- [ ] 检查邮箱收到验证码
- [ ] 输入验证码、密码、昵称
- [ ] 点击注册，确认跳转到仪表盘

#### 用户登录流程
- [ ] 访问 http://localhost:3000/login
- [ ] 输入邮箱和密码
- [ ] 点击登录，确认跳转到仪表盘

#### 项目管理
- [ ] 访问 http://localhost:3000/projects
- [ ] 点击"新建项目"
- [ ] 填写项目信息并保存
- [ ] 进入项目详情页
- [ ] 测试项目编辑和删除

#### 章节编辑
- [ ] 进入项目详情页
- [ ] 创建新章节
- [ ] 测试富文本编辑器
  - [ ] 粗体、斜体、下划线
  - [ ] 撤销/重做 (Ctrl+Z / Ctrl+Y)
  - [ ] 全屏模式 (Ctrl+Shift+F)
  - [ ] 自动保存
- [ ] 测试字数统计

#### AI 生成功能
- [ ] 在章节编辑页点击 AI 助手
- [ ] 选择续写/润色/扩写等操作
- [ ] 检查 AI 生成内容
- [ ] 测试自定义提示词

#### 数据导出
- [ ] 在项目详情页点击导出
- [ ] 测试 TXT 格式导出
- [ ] 测试 Markdown 格式导出
- [ ] 测试 HTML 格式导出

#### 会员系统
- [ ] 访问会员中心页面
- [ ] 测试兑换码激活

#### 管理后台（需要管理员账号）
- [ ] 访问 http://localhost:3000/admin
- [ ] 查看统计数据
- [ ] 测试用户管理功能
- [ ] 测试兑换码管理

---

## API 测试

### 使用 curl 测试

#### 发送验证码
```bash
curl -X POST http://localhost:3000/api/auth/send-code \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}'
```

#### 用户注册
```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email":"test@example.com",
    "password":"123456",
    "code":"123456",
    "name":"测试用户"
  }'
```

#### 获取项目列表（需登录）
```bash
curl -X GET http://localhost:3000/api/projects \
  -H "Cookie: next-auth.session-token=YOUR_TOKEN"
```

---

## 单元测试

### 运行测试

```bash
# 运行所有测试
npm test

# 运行特定测试文件
npm test -- register.test.ts

# 监听模式
npm test -- --watch

# 测试覆盖率
npm run test:coverage
```

### 现有测试文件

| 文件 | 测试内容 |
|------|----------|
| `src/__tests__/api/register.test.ts` | 注册 API |
| `src/__tests__/api/projects.test.ts` | 项目 API |

---

## 邮件测试

### 测试验证码发送

1. 启动项目 `npm run dev`
2. 访问注册页面
3. 输入真实邮箱地址
4. 点击发送验证码
5. 检查邮箱是否收到验证码邮件

### 邮件配置确认

```env
MAIL_SERVER="smtp.163.com"
MAIL_PORT="465"
MAIL_USE_SSL="true"
MAIL_USERNAME="wangTest321@163.com"
MAIL_PASSWORD="WVuUuxnuBBiWqi2x"
MAIL_DEFAULT_SENDER="墨飞小说创造<wangTest321@163.com>"
```

---

## 性能测试

### 页面加载时间

```bash
# 使用 lighthouse
npx lighthouse http://localhost:3000 --view
```

### API 响应时间

在浏览器开发者工具 Network 面板查看 API 响应时间。

---

## 测试账号

### 创建管理员账号

```bash
# 进入数据库
docker exec -it postgres psql -U novelist -d novelist_dev

# 更新用户为管理员
UPDATE "User" SET role = 'ADMIN' WHERE email = 'your-email@example.com';
```

---

## 测试检查清单

### 功能测试
- [ ] 邮箱验证码发送
- [ ] 用户注册
- [ ] 用户登录
- [ ] 项目 CRUD
- [ ] 章节编辑
- [ ] AI 生成
- [ ] 数据导出
- [ ] 会员激活

### UI/UX 测试
- [ ] 明暗主题切换
- [ ] 响应式布局
- [ ] 加载状态
- [ ] 错误提示
- [ ] 表单验证

### 安全测试
- [ ] 未登录访问受保护路由
- [ ] 普通用户访问管理后台
- [ ] SQL 注入防护
- [ ] XSS 防护

---

## 常见问题

### 1. 测试失败：数据库连接

确保数据库服务正在运行：
```bash
docker ps | grep postgres
```

### 2. 测试失败：邮件发送超时

检查网络连接和 SMTP 配置。

### 3. 测试失败：认证过期

重新登录获取新的 session。
